# Bidframe — Live Team Status

> **Read this first, every session.** It's the single source of truth for *where the project is right now*.
> Owned + kept current by **J** (glue/standup), but **every role updates the line for their own area when they push.**
> For the *fixed* stuff — schema, git rules, pipeline — see [AGENTS.md](AGENTS.md) and [tender-master-plan.md](tender-master-plan.md). This file is the *moving* part.

**Tool name:** **Bidframe** (locked Day 1). _Auditable requirement breakdown + compliance matrix → grounded autofill of the bid response, for SME public-sector bidders._

> ✅ **SCOPE: auditable autofill — RATIFIED + schema merged to `main` (2026-06-28).** Bidframe extends to **end-to-end bid drafting**, messaged as **"auditable autofill"** (never "we write your bid"). Write-up + role implications: [autofill-scope-decision.md](autofill-scope-decision.md). Extraction + the disqualifier catch stay the spine and the Day-4 gate; autofill rides on top, ships only once that gate is green. Per-lane mirror tasks are in "The locked contract" below.
>
> 💬 **Agent comms live:** read all four `comms/board-*.md` + this file on startup; post to your own board. Protocol: [comms/README.md](comms/README.md).
**Track:** Conduct "Make Legacy Move" (primary). Fetch.ai stack = **❌ NO-GO** (decided Day 3 — focus stays on the core demo, not a second stack we'd half-integrate).
**Timeline:** Day 1 = **28 Jun 2026** · Demo = **4 Jul** · **Day 4 = integration gate** (end-to-end on a fresh tender; not working by EOD4 → Day 5 cuts scope, doesn't add).
**Last updated:** 2026-07-01 (Day 4) by J.

---

## The one rule that keeps us fast

Work **directly on `main`**, in **your own folder**. Loop constantly:

```bash
git pull --rebase      # before you start AND before you push
# ...focused change in YOUR lane...
git add -A && git commit -m "clear message"
git pull --rebase
git push
```

**When you push a meaningful change, update your row in "Where each role is" below.** Pull often so you see others' updates. Branch+PR only for: (1) a schema change, (2) a big/risky refactor. Full rules: [AGENTS.md](AGENTS.md) / [CONTRIBUTING.md](CONTRIBUTING.md).

---

## The locked contract (do NOT change without team agreement)

The **requirement object** schema in [AGENTS.md](AGENTS.md) §"Data contract" is **locked**. Frontend builds mock data in it; backend + generalist produce it for real. Changing it normally needs team sign-off (it breaks both sides at once).

`{ id, text, source_page, source_clause, source_excerpt, type, is_gating, category, confidence, status, needs_review, decision, criteria_ref, depends_on, draft_answer, answer, open_questions }` + `capability_docs[]` on the tender response.

> ✅ **SCHEMA EXTENDED — team-confirmed 2026-06-28, now on `main`.** Added (additive, nullable — the matrix UI is unaffected): `answer` `{ text, state, evidence_refs[], confidence }`, `open_questions[]`, and `capability_docs[]` on the tender response. `draft_answer` kept as a deprecated alias of `answer.text`. Rationale + field details in [autofill-scope-decision.md](autofill-scope-decision.md). *(Team waived the PR ceremony for this one — verbal sign-off was the point of the rule; see [CONTRIBUTING.md](CONTRIBUTING.md).)*
>
> **Each role's agent — mirror it in your lane:**
> - **Frontend:** add the fields to `frontend/src/types/requirement.ts` + a couple of `mock-requirements.ts` examples. Safe to ignore in the matrix until the answer UI is built.
> - **Backend:** add `answer` / `open_questions` / `capability_docs` to your request/response models + serializers (nullable/empty by default).
> - **Generalist/J:** wire the `prompts/answer-generation.md` + `prompts/gap-interview.md` prompts into the pipeline (after the extraction Day-4 gate).

**Intermediate format (backend → generalist):** the *raw extraction list* — requirement objects pre-reconcile (cross-chunk duplicates allowed, raw confidence, ids not yet deduped). **Spec is up — `prompts/raw-extraction-format.md` (PROPOSED v1)** with a 6-item mock at `prompts/mock-raw-extraction.json`. **Backend + generalist: review + sign off in standup today**, build against the mock meanwhile.

---

## Where each role is (each owner edits their own row)

| Role | Owns | Status | Next | Blocked on |
|------|------|--------|------|-----------|
| **Backend** | PDF ingest · chunk · extract · classify · graph · SQLite · REST API | 🟢 **pipeline built + tested** (J covering, backend's laptop down till tomorrow) — ingest→chunk→extract→SQLite, all 3 endpoints live, tested on SPSO (20 reqs). Heuristic extractor (no key); Claude path ready | **Backend, when back:** swap heuristic→Claude, harden ingest (tables/OCR), graph edges, hand raw list to generalist. See `backend/README.md` "Owner TODOs" | nothing |
| **Generalist** | reconcile/dedupe · confidence routing · eval harness · answer-draft | 🟢 **all 4 lane items shipped + autofill LIVE, fast & proven** — `engine/` on main (**116 tests**): reconcile **live in `backend/pipeline.py`** (conservative dedupe + noisy-OR + safety escalation); `needs_review` **calibrated 0.70** (G-007); **eval harness + aggregate `eval_all`** (SPSO **recall ~0.9 / gating recall 1.0 / 0 dangerous**); **auditable autofill wired into the live API** — `GET /requirements` returns grounded answers + gaps (mock on upload), `POST /tenders/{id}/draft` (OpenAI, **parallelized + `?limit`**, capability-doc upload); **groundedness eval proves 0 bluffs** (SPSO 32 grounded / 96 gaps / 42-42 citations verified); **sharper gap questions** via J's prompt (G-010..G-012); 66pp tender survives extract→reconcile; **adversarial trust-invariant suite** — the 4 trust claims hold under attack (G-015) · **Day-4 (07-01, G-016..G-025):** recovered the OpenAI key + **verified the full live path e2e** (health→openai, async upload→job→done, both disqualifiers, `/draft` 0 bluffs); **fixed a demo-blocking bug** (a shadowed `_run_extract_job` silently crashed every async upload); **pre-baked SPSO + NHS demo fixtures** (`frontend/src/data/*-prebake.json`, key-independent); **display-dedupe** (matrix/hero) + **richer capability docs** (autofill grounds 48→109); SPSO gold **signed off**; museum gold **rejected as mis-sourced** | **@frontend: wire the pre-baked SPSO/NHS fixtures into `/demo`** (committed, ready) + the small `getTenders`/`?limit` diffs (G-016/G-021); **@j: set `OPENAI_API_KEY` on Render** (deployed still runs heuristic) — the pre-bake makes the demo key-independent regardless | not blocked |
| **Frontend** | compliance matrix · source panel · decision controls · graph view · demo · landing | 🟢 **civic record now spans the whole app** — **graph view** (`/graph`) rebuilt off default React Flow into a bespoke relationship map (register cards + recessed criterion tabs, centroid roll-up layout, token-coloured edges, ledger canvas, drawing legend); **masthead** nameplate (BIDFRAME running head + real reference line + one 2px ink rule), **upload** as a lifted grained sheet, **requirement panel** gains the ruled margin + pressed evidence blocks + the **approval stamp on approve**; `ApprovalStamp` promoted to a shared component (F-015). Prior: hi-fi **landing** at `/` (matrix `/review`), tilted real-product hero, worked-example entry (F-013); answer/evidence panel, gap-interview (`/answers`), live API wiring (mock by default). tsc + lint + `next build` green · **Day-4 (07-01), all on `main`:** auth wired into the FE (login page, route gate, header sign-out, landing "Sign in"), **multi-file upload** + a `/tenders` list, QOL (keyboard nav, graph deal-breaker filter, refresh-safe restore, evidence library), live-product **empty states** + "verified N of M" count + responsive header + honest draft fallback, "See the demo" promoted to the masthead; **`/demo` now runs the real pre-baked SPSO run** (G-021 — its own frozen provider seeds `spso-prebake.json`, so the showcase shows a real tender key-independently and the hosted live/mock state can't leak in) + **landing polish landed** (microinteractions, drawer overflow fix, `GatingHero` track-record line, copy/em-dash fixes, new `Reveal`). tsc + lint + `next build` green | **Build the graph reframe + claim-verification** per [`graph-and-verification-deep-plan.md`](graph-and-verification-deep-plan.md) (J-043) — start client-side (PDF.js text-layer highlight), no backend needed. Set `NEXT_PUBLIC_API_BASE_URL` → Render for hosted live data. *(NHS 66pp prebake also available as a second demo tender.)* | nothing blocking |
| **J** | prompts · orchestration · narrative · traction · glue | 🟢 **ahead** — 4 prompts (**gating fix verified, G-004**), raw-extraction contract, autofill scope+schema, comms, standup; scaffolded + **deployed the backend to Render**; **traction dossier DONE** (`traction-research.md`: 19 CRM targets, 14 verified handles, every sector + outreach kit); **demo narrative LOCKED** (Day 3); **G-009 `render.yaml` flipped to `rootDir: .`** (engine now on the deployed path); **Fetch.ai = NO-GO** · **Day-4 (07-01):** shipped the invite-only **auth system** (PR #15), **consolidated every branch onto `main`** (multi-file #4 + QOL + UX fixes, integrated with auth, no open PRs), wrote the **graph/verification deep plan** + `/demo` scrollytelling pack and **flagged the build to @frontend (J-043)** | **Set `OPENAI_API_KEY` + `AUTH_SECRET` on Render** (deployed still heuristic, and now needs the auth secret + accounts to run gated); **start traction outreach** (dossier ready, demo-solid); rehearse the 90-sec script | **OpenAI key** (organisers) |

---

## Open decisions / watch list

- **Autofill scope** — ✅ **ratified + schema merged to `main`** (`answer`/`open_questions`/`capability_docs`); prompts drafted (`prompts/answer-generation.md`, `prompts/gap-interview.md`). The answer-draft pipeline step is **Day-3** work (generalist + J). **Must not delay the extraction Day-4 gate.**
- **LLM provider** — ✅ **OpenAI** (hackathon OpenAI/Codex credits). Backend extractor defaults to OpenAI when `OPENAI_API_KEY` is set (`LLM_MODEL` default `gpt-4o`); heuristic runs without a key; Claude kept as an alt. Prompts stay provider-agnostic.
- **Sourcing sprint (Day 1, all four)** — grab 10–15 real UK public-sector tenders. ✅ **Hour-one check DONE**: SPSO cleaning ITT sourced + parsed clean (13pp). Use direct-download ITTs (no portal approval) — see [tenders.md](tenders.md). Save PDFs to `data/tenders/` (gitignored). Still want more for the gold set + ugly-tender stress tests.
- **Gold set (by EOD Day 2)** — each person hand-labels ONE tender end-to-end.
- **Fetch.ai stack** — ❌ **NO-GO (decided Day 3, 2026-06-30).** Core demo is the priority; we won't half-integrate a second stack. Revisit only if the demo is fully locked with real slack to spare.

## Recently shipped (newest first)

- **2026-07-01** — Frontend: **`/demo` now runs the real pre-baked SPSO tender (G-021) + landing polish landed.** The read-only showcase no longer shows the fabricated `mockTender` — `/demo` mounts its own `RequirementsProvider` seeded from `frontend/src/data/spso-prebake.json` (the real gpt-4o extract+autofill: 183 reqs · gating recall 1.0 · 109 grounded · 0 bluffs), so it shows a **real public-sector tender key-independently**, no backend or API key on stage. The seeded provider is **frozen** (skips the sessionStorage live-tender restore + exposes `title` for the header), so the hosted build's live/mock state can't leak into `/demo`; every other screen is untouched. The 183 raw reqs collapse to unique rows via the existing display-dedupe. Landed the pending frontend-polish branch alongside (landing microinteractions, drawer overflow fix, `GatingHero` track-record line, copy/em-dash fixes, new `Reveal`), reconciling cleanly over the Generalist oxblood-token retune (kept the new `signal-oxblood-frame` + added `card-live`). tsc + lint + `next build` green; `/demo` prerender verified to carry the SPSO title + reqs. **Next: J-043** (graph reframe + PDF.js claim-verification). *(Codemap left to CI — the local generator needs Python 3.10+.)*
- **2026-07-01** — Generalist: **frontend UI/UX design uplift shipped** (cross-lane, Bobby-directed; comms **G-027**, full spec [`frontend/design-uplift.md`](frontend/design-uplift.md)). Deal-breaker **alarm** (signal-oxblood `#8A2D2A→#B42D24` + new edge-only `signal-oxblood-frame` token, full-fill "Can't answer" bead + `!` glyph, 3px gating spine); the **completion win-moment** (record-filed sheet + approval stamp — closes ux-audit #8); a **`/thank-you`** return page + new-tab "Book a demo" CTAs; a differentiated **status column** (killed the "Needs your eye" wall → blank / Deal-breaker to clear / Needs your answer / Worth a second look). Merged cleanly over the display-dedupe. `eslint` + `next build` green; eyeballed on `/demo` + `/thank-you`. ⚠️ the oxblood retune is a **locked-token change** — @j/@jawad please sanity-check (pushed on Bobby's call). External: @joel sets Cal.com *Redirect on booking* → `/thank-you`.
- **2026-07-01** — J: **Day-4 consolidation — everything on one `main`, no open PRs.** Merged the tested `test/tonight` branch into `main`: **multi-file tender packs (#4)** (additive/nullable `source_docs`, per-doc `?doc=` PDF serving, pack upload) **+ the QOL polish** (`/tenders` list, keyboard nav, graph deal-breaker filter, refresh-safe restore, evidence library), hand-resolving the **auth × multi-file** overlap so all three coexist — and caught an auto-merge bug that had dropped auth's `owner` from the async upload job (would've crashed every upload). Then reconciled + landed the **matrix/graph/answers/landing UX fixes**: live-product **empty states** (no more sample data before upload), a **"Bidframe verified N of M — K need your input"** line, a responsive header, an **honest draft fallback** when the server has no AI key (was a misleading "couldn't reach server"), and **"See the demo" promoted to the masthead**. Verified each step (backend imports + **32/32** auth+owner+multi-file+`source_docs` checks; FE lint + `next build`, all routes). **New: a deep design plan for the two rough edges** — [`graph-and-verification-deep-plan.md`](graph-and-verification-deep-plan.md) (~15 options each: the graph reframed around *"where the marks live"*; claim verification as a split-screen with the **exact line highlighted**) — **flagged to @frontend as a build (J-043)**, with a `/demo` scrollytelling design pack alongside ([`demo-scrolly-design-pack.md`](demo-scrolly-design-pack.md)). PRs #13/#15/#16 merged, #14 closed-by-integration.
- **2026-07-01** — Generalist: **live OpenAI path verified e2e + a demo-blocking bug fixed + demo refinements** (G-016..G-025). Recovered the OpenAI key (was on the other laptop) → verified the **full live path** end-to-end (`/health`=openai, async upload→job→`done`, both SPSO disqualifiers caught, `/draft?provider=openai` grounded, **0 bluffs**). **Fixed a bug that silently broke every async upload** — a duplicated `_run_extract_job` (docs- vs `pdf_path`-based) shadowed the correct one so `POST /tenders/upload`'s background job crashed on every upload; the tests missed it because they use `?sync=1`. **Pre-baked SPSO (hero) + NHS-66pp (messy-proof) demo fixtures** (`frontend/src/data/*-prebake.json`) — real gpt-4o, **key-independent**, so the demo needs no live key on stage (J-020 done). **Demo-clarity refinements:** display-level dedupe of duplicate/gating rows (`frontend/src/lib/dedupe.ts` — gating collapses on **exact text only**, never fuzzy, so a disqualifier is never hidden; caught by an adversarial pass) + **richer AcmeClean capability docs** (autofill grounds **48→109** of 183, still 0 bluffs). SPSO gold **signed off** (verified pp.1-6); **museum gold rejected as mis-sourced** (SPSO labels on the MAC PDF — eval-manifest note corrected). Full suite **116 green**; gating recall **1.0** / 0 dangerous / 0 bluffs held throughout. **Still open:** @frontend wire the fixtures into `/demo`; @j set the Render key (deployed still heuristic). Local test creds: `demo@bidframe.co.uk` / `demo123456`.
- **2026-07-01** — J: **invite-only account system shipped** (self-hosted JWT auth — PR #15, **merged to `main`** as `84b4e76`). Bidframe is a paid product, so the live app + API are now **gated**: no public signup, accounts made via `python -m app.admin create-user`; **every `/tenders` + `/requirements` endpoint requires a bearer token and is owner-scoped** (another user's data reads 404 — enforced on the server, not just hidden in the UI). Backend: `auth.py` (stdlib pbkdf2 hashing + PyJWT sessions + `current_user` guard), `admin.py` CLI, a `users` table + per-tender `owner` column; PDF endpoint also takes `?token=` for `<iframe>` opens. Frontend: `AuthContext` (validates via `/auth/me`), `/login` page, `AuthGate` on upload/review/answers/graph, header sign-out, landing **Sign in** link; token in localStorage, sent on every call. **Mock/demo build (no `NEXT_PUBLIC_API_BASE_URL`) stays open**, so `/demo` + the current Vercel showcase are unchanged — the gate engages only where a live backend is wired (also closes the open-upload credits risk). New dep `PyJWT`; new env `AUTH_SECRET` (insecure dev default → set a strong one on deploy). **30/30 backend auth checks + FE lint/`next build` green.** **Team login accounts** — created on the local test instance (⚠️ dev/demo creds: rotate + recreate on deploy, never commit prod passwords): Backend `backend@bidframe.co.uk` / `bidframe-backend-2026` · Generalist `generalist@bidframe.co.uk` / `bidframe-general-2026` · Frontend `frontend@bidframe.co.uk` / `bidframe-frontend-2026` · J `j@bidframe.co.uk` / `bidframe-j-2026`. Running locally now: FE http://localhost:3300, API :8000.
- **2026-06-30** — Frontend: **graph view rebuilt + civic record swept across the rest of the app** (F-015). `/graph` is no longer default React Flow: bespoke relationship map with register-card requirement nodes (shared confidence bead, oxblood gating edge, mono clause ref), recessed criterion tabs, a centroid layout that turns spaghetti into clean roll-ups, token-coloured edges (pencil / oxblood / forest-dashed) with arrowheads, a graph-paper ledger canvas, a bespoke mono zoom control and a drawing-style legend. App-wide material pass: the **masthead** is now a nameplate (BIDFRAME running head, Fraunces title at 24, a real reference line, one 2px ink rule), the **upload dropzone** is a lifted grained sheet, the **requirement panel** (split + drawer) gains the lifted grainy sheet + ruled margin + pressed evidence blocks (`--paper-recessed`/`--depth-pressed`) + the **approval stamp on approve**; `ApprovalStamp` promoted to a shared component. No new hues; forest stays the one earned accent. tsc + lint + `next build` all green, every route static. (Graph edges/fit need a real browser; the headless preview here has no `ResizeObserver`, so structure + node coordinates were verified programmatically.)
- **2026-06-30** — J: **G-009 deploy fix applied + demo script locked + Fetch.ai called.** Flipped `render.yaml` to `rootDir: .` (+ `pip install -r backend/requirements.txt`, `uvicorn backend.app.main:app`) so the deployed API imports `engine/` and runs the **real** reconcile/autofill instead of the placeholder (generalist verified this fix end-to-end in G-009). **Still key-gated:** the deploy needs `OPENAI_API_KEY` set in the Render dashboard + a redeploy to actually catch disqualifiers (heuristic = gating recall 0.0). Locked `demo-narrative.md` (beats + honest numbers frozen, rehearse to it). **Fetch.ai = NO-GO** (focus on the core demo). Comms: J-026.
- **2026-06-29** — Generalist: **Day-5 hardening (safe) — adversarial trust-invariant suite** (G-015). 18 judge-style attack tests (**116 total green**) prove the 4 demo claims hold: conservative reconcile never mis-merges (page/clause/null/token gates), disqualifiers never downgraded, autofill never bluffs (detector catches a planted fake citation), the eval can't hide a gating miss. Additive tests only — no behaviour change. One honest known limit documented (lexical reconcile can merge same-page+clause near-identical reqs; the AND-gate mitigates).
- **2026-06-29** — Generalist: **Day-4 "break it" pass — locked numbers + caught a bad gold set** (G-014). **Robustness: 7/7** ugliest real tenders survive ingest→extract→reconcile→autofill (incl. 66pp NHS / 472 reqs, 53pp security+cleaning / 364 reqs) — no crashes. **Locked headline:** gating recall 1.0 · 0 dangerous misses · 0 bluffs (overall recall ~0.79–0.95 = gpt-4o noise; quote the disqualifier catch, not the wiggly %). **⚠️ `museum` gold quarantined** (`draft:true` in eval-manifest): it's auto-generated/unvalidated (88/92 marked gating, non-schema type, string pages) → manufactured a fake "87 dangerous misses"; **needs a human cleanup** before it can score. Demo rec: hero = SPSO, messy proof = NHS 66pp.
- **2026-06-29** — Generalist: **auditable autofill is LIVE in the API + hardened for the demo** (`engine/` + `backend/app/`, 98 tests). `GET /requirements` now returns grounded answers + gaps (free mock answerer on upload); new **`POST /tenders/{id}/draft`** for precise OpenAI grounding — **parallelized** (minutes→seconds) + `?limit` + capability-doc upload. **Groundedness eval** (`engine/eval_answers.py`) proves *never bluffs*: SPSO = 32 grounded / 96 gaps / 42-42 citations verified / **0 bluffs**. **Sharper gap questions** via J's `gap-interview.md`. Frontend "Autofill with AI" button + capability-upload panel on `/answers`. **Found + handed J the deploy gap (G-009):** the Render `rootDir: backend` means the *deployed* API silently runs the placeholder reconcile + heuristic (0 disqualifiers, no autofill) — flip `render.yaml rootDir: .` + set Render `OPENAI_API_KEY` to go live. Comms G-009..G-012.
- **2026-06-29** — J: **backend deployed live to Render** — public API at `https://bidframe-api.onrender.com` (Render Blueprint from `render.yaml`; heuristic extractor, no key needed). Verified end-to-end (`/health` → `{"status":"ok","extractor":"heuristic"}`, `/docs` → 200). Answered frontend's F-003 with the URL + Vercel env step (comms **J-016**); frontend sets `NEXT_PUBLIC_API_BASE_URL` to make the *hosted* demo show live data. Free tier sleeps on idle + SQLite resets on redeploy (fine for demo); auto-upgrades to GPT when `OPENAI_API_KEY` is added in the Render dashboard.
- **2026-06-29** — Generalist: **`engine/` shipped — reconcile/dedupe + eval harness** (pure-Python, **60 tests green**). Conservative AND-gate merge (char-sim + token-Jaccard + page + clause) with anti-transitivity guard + noisy-OR confidence; 6 raw → 5 final on the mock (cross-chunk ISO-9001 dupe merges). Closed reconcile→eval loop scores recall/precision/gating **1.0 on the mock gold**; eval flags missed gating reqs as DANGEROUS (no LLM judge — deterministic, auditable). Signed off the raw-extraction format (G-001). **Then (same day):** ran the full PDF→extract→reconcile→eval loop on real SPSO pp.1-6 (`engine/scripts/run_tender.py`, 64 tests) → **recall 0.95 (18/19), gating recall 1.0, 0 dangerous misses** — the first real headline number.
- **2026-06-28** — Frontend: **applied the locked typeface across the UI** (typography-only pass, shipped via PR). Wired the §11 type system via `next/font`: **Fraunces** headings, **Chillax** body (self-hosted from Fontshare under `src/fonts/`), **IBM Plex Mono** for evidence + source references (page/clause refs, IDs, matrix column headers, kicker labels). Tokens are `--font-head`/`--font-body`/`--font-mono` mapped to Tailwind `font-serif`/`font-sans`/`font-mono`; the six-size scale applied to headings. No colour/layout/motion changes. Build + lint green; `@font-face` + tokens verified in the compiled CSS.
- **2026-06-28** — Frontend: **applied the Bidframe colour system across the UI** (colours-only pass, shipped via PR). The 11 tokens from `design/colours.html` are now Tailwind v4 `@theme` tokens in `globals.css` (brand chrome vs status signal, kept strictly apart); every component moved off the placeholder slate/red/amber/emerald/blue palette. Confidence dot now maps the 4-tier signal scale; gating = oxblood; no off-palette colour or pure white/black surfaces remain. Build + lint green. Type / layout / motion / anti-slop are still to come; the design-system docs + typography specimens are Jawad's in-progress work (not in this PR).
- **2026-06-28** — Frontend: **wired to the live backend** (first frontend↔backend integration; shipped via **PR**, not direct-to-main, for visibility). New `src/lib/api.ts` calls `POST /tenders/upload`, `GET /tenders/{id}/requirements`, `PATCH /requirements/{id}`. Env-driven via `NEXT_PUBLIC_API_BASE_URL` — **mock by default** (deployed demo stays zero-surprises), real data when the env is set (local `:8000` or the Render URL). Upload flow now does a real upload→extract→load into the matrix (with an error state); approve/edit/flag persist via PATCH. Added `frontend/.env.example`. Build + lint green. **Backend API confirmed reachable per `frontend-integration.md`.**
- **2026-06-28** — Frontend: **answer + evidence panel + gap-interview UI** shipped — the auditable-autofill payoff. The requirement drawer now renders each drafted **answer with its capability-doc evidence citations** (filename · page · verbatim excerpt) + visual confidence + inline edit; new **`/answers`** route + nav item is the **gap-interview to-do list** (open questions answerable inline, with an autofill summary + progress bar). Mock enriched with 3 more drafted answers (2 `needs_input` with real open questions). Build + lint green; the matrix visual language is untouched. **Next:** design-system pass + swap to the live backend API (`frontend-integration.md`).
- **2026-06-28** — Frontend: **GitHub auto-deploy wired** — Vercel now builds on every push (the Welddevelopment org couldn't grant the Vercel app, so deploys mirror via a personal `cosmosmarkets/TenderBreak` repo with Root Directory `frontend`). My local `origin` push-mirrors to both the team repo **and** the deploy repo, so one `git push` ships code + triggers the deploy. Team source of truth stays Welddevelopment.
- **2026-06-28** — Frontend: **rebranded UI to Bidframe** (locked Day-1 name) + **mirrored the additive autofill schema** (`answer`/`open_questions`/`capability_docs`) into `types/requirement.ts` with worked mock examples (one auto-drafted answer, one `needs_input` + gap question). Build green; matrix unaffected.
- **2026-06-28** — Frontend: **wireframed + deployed the remaining sections** (separate routes + persistent navbar; source+decision slide-over drawer with approve/edit/flag; gating "deal-breaker" hero; React Flow relationship graph over `criteria_ref` + `depends_on`; drag-drop upload flow). Decisions held in in-memory context (no localStorage, per brief) and survive navigation. Mock enriched (dependency edges + 2 decided sample states). Live on Vercel. **Next:** rebrand UI to Bidframe + mirror the additive autofill schema fields.
- **2026-06-28** — J (covering for backend, with their OK — laptop down till tomorrow): **full backend pipeline built + tested end-to-end** (`backend/app/`: ingest, chunk, extract, pipeline, store, schema, wired endpoints). 20 reqs extracted from SPSO + persisted via API. Pluggable extractor (heuristic now, Claude on key). Frontend can integrate against the real API.
- **2026-06-28** — ✅ **Hour-one parse check PASSED on a real tender** (SPSO cleaning ITT, 13pp clean). Biggest Day-1 engine risk retired. Sourcing log started: [tenders.md](tenders.md).
- **2026-06-28** — J (covering for backend, with their OK): `backend/scripts/parse_check.py` — hour-one tender parseability gate (PyMuPDF→pypdf fallback); added `pymupdf` to requirements. **Tested + working** (installed Python 3.12; clean→PASS, image-only→needs-OCR, no-arg→usage). Fixed a Windows cp1252 emoji crash.
- **2026-06-28** — J: **agent comms channel** (`comms/`) — per-role boards, conflict-free; wired into AGENTS.md + STATUS startup reads. Role table refreshed to real progress.
- **2026-06-28** — Schema extended for autofill (`answer`, `open_questions`, `capability_docs`) — team-confirmed, merged to `main`. Per-lane mirror tasks listed in "The locked contract" above.
- **2026-06-28** — J: **autofill scope decision** + `prompts/answer-generation.md` + `prompts/gap-interview.md` (auditable autofill: grounded per-requirement answers + deduped gap questions). Pending team ratification + schema PR.
- **2026-06-28** — J: v1 extraction + classification prompts (`prompts/extraction.md`, `prompts/classification.md`) — provider-agnostic, recall-first, structured-output schemas inline.
- **2026-06-28** — J: raw-extraction format spec + 6-item mock (`prompts/raw-extraction-format.md`, `prompts/mock-raw-extraction.json`) — backend→generalist contract.
- **2026-06-28** — J: cleaned up stray global config; locked tool name **Bidframe**; created this `STATUS.md`.
