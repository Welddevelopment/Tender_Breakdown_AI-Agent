fastapi
uvicorn[standard]
pydantic
python-multipart
python-dotenv
pymupdf
pdfplumber
pypdf
# Mixed tender-pack ingestion: Word / Excel readers. CSV uses stdlib.
python-docx
openpyxl
# Auth: JWT session tokens (self-hosted, invite-only). Passwords use stdlib pbkdf2.
PyJWT>=2.8
# LLM extractor. OpenAI is our chosen provider (hackathon credits) — set OPENAI_API_KEY.
# The heuristic extractor runs without any key. anthropic is an optional alternative.
openai
anthropic
# Production queue worker (worker.py): Supabase Postgres + Storage REST.
psycopg[binary]
httpx
